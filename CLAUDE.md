Use AGENTS.md for information about agents and their capabilities.

## Vault de Contexto

Vault Obsidian: `C:\Users\Usuario\Documents\Carraca`

**Antes de qualquer tarefa:** ler `00 - Índice.md` para orientação.
Usar `[[wikilinks]]` do vault para navegar diretamente à nota relevante.

## Regras de Manutenção do Vault

- Mudou comportamento do código → atualize a nota correspondente na mesma sessão
- Errou e foi corrigido → registre em `Armadilhas.md` com causa raiz (não só o sintoma)
- Escolheu entre duas abordagens → registre em `Decisões.md`
- Nunca edite uma nota sem reler os links que apontam para ela
- Novo campo em `PlannerProfile` → bump `PRESET_SCHEMA_VERSION` em `lib/schema.ts` + atualizar `Versionamento de Schema.md`
